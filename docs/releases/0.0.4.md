# Salt All The Things 0.0.4

## Highlights

- Adds the server-owned Song Bank data contract and persistence foundation for manually curated episode songs while keeping private host notes authenticated and out of public episode responses.
- Adds an authenticated, responsive Song Bank page for the complete manual create, review, edit, filter, retire/restore, unassign, and delete workflow.

## Fixes/Changes

- Stores each song as an independent record with artist, title, validated official YouTube URL, private notes, lifecycle state, optional episode-idea assignment, and server-owned timestamps.
- Adds authenticated, revision-guarded Song Bank read/write, import/export, assignment, replacement, removal, retirement, restoration, and deletion behavior.
- Enforces one song per idea and one active idea per song through application transactions and database constraints; idea deletion frees its song in the same transaction.
- Extends the browser storage contract with Song Bank cache, import/export, and atomic lifecycle methods without adding a public Song Bank response.
- Adds Song Bank navigation to authenticated crew pages, actionable client/server validation and conflict recovery messages, confirmation for destructive or assignment-affecting actions, assignment context, escaped rendering, and accessible status/filter controls.
- Packages and serves `songs.html` and its script from the same immutable application image used by the API.

## Validation

- The complete safe local suite passed 171 tests; 79 database-backed cases skipped safely because this workstation has no configured isolated `TEST_DATABASE_URL`, Docker runtime, or PostgreSQL service.
- Song contract and Alembic-chain coverage passed locally, including supported and rejected YouTube URL forms with network access disabled; Python compilation, changed-file lint, JavaScript syntax/storage contracts, SQLAlchemy mapper configuration, release validation, and diff integrity also passed.
- Song Bank page coverage passed locally for supported/rejected URLs, required fields, escaped output, filtering, assignment labels, navigation, accessible status/error controls, revision-guarded storage routes, same-origin isolation, page serving, and production static-asset packaging.
- Pull-request validation passed 250 tests against fresh isolated PostgreSQL through revision `0007`, plus production image inspection, `0007` to `0006` to `0007` rollback/recovery, backup/restore fingerprint continuity, and immutable health checks.
- Isolated development passed environment/version/commit health, migration `0007`, registration/login/protected/public checks, persistence, and cleanup. Its bounded Song Bank smoke also passed invalid-URL rejection, stale-conflict protection, create/assign/replace/reload, private-note exclusion from public responses, retire/restore, idea-deletion cleanup, record deletion, and temporary-data removal.

## Deployment/Migrations

- Adds Alembic revision `0007`, which creates `satt.songs` with lifecycle, foreign-key, check, and uniqueness constraints.
- Development, test, and pull-request migration verification advances to head `0007`; application startup continues to apply migrations before serving traffic.
- Production continuity fingerprinting treats the additive table as empty before migration and includes every Song Bank field through one-way counts and SHA-256 evidence after migration.
- No external YouTube, AI, OAuth, Drive, DNS, environment, secret, or production configuration change is required.

## Rollback

- Application rollback remains the prior immutable commit through the documented non-production Compose recovery path.
- Downgrading `0007` to `0006` drops only the new `satt.songs` table, but permanently removes Song Bank records; export or database backup is required before a downgrade once song data exists.

## Known Limitations

- The authenticated Song Bank page manages records and can remove an existing assignment, but episode selection and private preparation display remain in child #23; the Spotify-ready public song block remains in child #24.
- YouTube URLs are validated locally as official HTTPS watch, short, live, embed, or `youtu.be` video links; the application intentionally performs no metadata lookup or remote availability check.
