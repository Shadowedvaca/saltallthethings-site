Got it — this sounds like a **separate post-production workflow**, not an extension of the pre-show planning flow.

Before I draft the Markdown reference for Claude Code, I need a few specifics about the existing technical side so I don’t make bad assumptions.

## Questions about the current app / data model

### Show records

1. What is the exact entity for an episode/show in your app right now? Is it `Idea`, `Show`, `Episode`, or something else?
2. Where is the “recorded” state stored today?

   * boolean flag
   * status enum
   * actual `recorded_at` datetime
   * scheduled slot with another field
3. Do you already store a **record date** separately from publish date / planned date?

### File matching

4. What is the current standard filename pattern you usually use for:

   * raw recording
   * finished episode
   * transcript
   * album art
5. Is there already a canonical episode slug/code in the DB that could be used for matching, or is filename matching currently outside the app?
6. When you said “I want to control the flow by it only matching episodes built on a file name,” do you want:

   * a single **canonical base filename** stored on the episode record, and all assets match against that
   * or automatic guessed matching first, with manual override per asset
7. Should the manual rename/edit live on the episode record as something like:

   * `asset_base_name`
   * `expected_file_stub`
   * `production_file_key`

### Asset storage

8. Where do these files live today?

   * local folders on your PC
   * synced Google Drive folders
   * uploaded to the server
   * a mix
9. Does the web app currently have filesystem visibility into any of those folders, or would this post-production tab need metadata pushed into the app from your local machine?
10. For album art, are you storing:

* generated image file path
* external URL
* OpenAI file/image ID
* nothing yet

### Transcript flow

11. Is there already a DB field for transcript text or transcript file path, or are transcripts currently just files in a folder?
12. When a cleaned audio file replaces the raw-based transcript, do you want to:

* overwrite the existing transcript record entirely
* keep transcript history/versions
* mark one as “current”

### UI / workflow

13. Do you imagine this new tab as a **single post-production queue**, where each row is an episode and columns are:

* Raw Dog Recording
* Finished Show
* Transcript
* Album Art
* Needed Next
* Match Name

14. Should the tab show only episodes with status `recorded`, or also `edited`, `published`, etc.?
15. When you say “shows marked as recorded on the current day or before, sorted descending by their record date,” should older unfinished episodes stay visible until all required assets are complete?

### Completion logic

16. For an episode to count as “complete,” do you want all four present?

* Raw Dog Recording
* Finished Show
* Transcript
* Album Art

17. Or is Raw Dog just an optional source once Finished Show exists?
18. What should “needed” mean exactly:

* missing required artifact
* stale artifact exists but needs regeneration
* filename mismatch needing manual intervention
* all of the above

### Matching + manual control

19. If the app finds multiple possible files for one episode, what should happen?

* show conflict state
* let you choose one
* pick newest automatically

20. If filenames do not match, do you want a manual action like:

* “Set base name”
* “Attach raw audio”
* “Attach finished audio”
* “Attach transcript”
* “Attach art”

### Technical integration boundary

21. Do you want Claude Code’s part to focus only on:

* the new tab / data model / matching logic / API contracts
  or also
* local watcher scripts that push file inventory into the app

22. Is your preference that the app **reads a scanned asset inventory** generated by your local machine, rather than the server trying to access local files directly?

Once you answer those, I’ll give you a clean **Markdown reference spec** for the ChatGPT-designed pieces only: the post-production flow model, matching rules, status logic, art-direction integration, and UI behavior.
