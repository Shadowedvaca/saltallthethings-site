# Salt All The Things 0.0.7

## Highlights

- Makes the initial AI-generated Top 3 proposal save path resilient to unrelated server revision changes without weakening real Top 3 concurrency protection.
- Keeps the homepage subtitle and Explore prompt separated as the hero content wraps across desktop, tablet, and mobile layouts.
- Opens the Schedule Board to the browser's current local month and year instead of a fixed March 2026 view.
- Starts every Show Management show collapsed so the user chooses which show to expand.
- Makes full show view mode available before or after a show is assigned to a schedule slot.
- Repaints authenticated management pages from the canonical server response after successful saves, failures, and conflicts so accepted changes appear immediately and rejected changes never remain presented as saved.
- Gives unscheduled, future scheduled, and past scheduled shows the same always-available Edit action without changing their schedule placement.

## Fixes/Changes

- Reconciles the canonical Top 3 Bank and current server revision immediately after AI generation while preserving the generated proposal in the workshop.
- Retries a conflicted bank mutation once only when the canonical Top 3 concept and assignment state is unchanged; an actual bank change remains visible and requires deliberate review and retry.
- Keeps one stable draft concept identifier across conflict retries so a recovered save cannot create a second proposal record.
- Preserves generated form values and provenance when a genuine conflict reloads the latest bank.
- Places the homepage Explore prompt in the hero's responsive content flow with bounded spacing instead of anchoring it over variable-height content.
- Initializes the Schedule Board calendar from the local date whenever Show Management opens or reloads.
- Keeps scheduled, unscheduled, newly processed, and recently edited show cards collapsed on load while preserving independent expansion and edit access.
- Gives scheduled and unscheduled cards the same full-show View action; unscheduled view uses an idea-scoped URL, labels the missing schedule, and does not create or change an assignment.
- Moves Edit to the common show-card action area so processed shows use one workflow whether they are unscheduled, scheduled in the future, or listed under Past Episodes; save and cancel leave slot assignment and date metadata untouched.
- Publishes canonical storage-state changes to Show Management, Config, Joke Bank, Song Bank, and Guest Bank renderers after each acknowledged mutation or rollback.
- Waits for release-date override and reset persistence before reporting success; a failed or conflicted save restores and displays the latest server state with retry guidance.
- Preserves previous and next month navigation, including December-to-January and January-to-December year transitions, without changing show slots or assignments.
- Aligns the shared Codex and Claude Code repository instructions with the Solo Development standard, including independent validation/integration inputs, cumulative Parent-cadence delivery, development-complete approval before any due human UI validation, four explicit gate types, and Mike-only exact version authority.
- Blocks production before SSH unless GitHub Actions proves the exact tagged SHA completed the SATT test deployment successfully from a `main` push; main ancestry alone no longer satisfies test provenance.
- Establishes canonical testing instructions, enforced Python coverage ratchets, and isolated Playwright journeys with bounded failure evidence.

## Validation

- Adds browser-contract coverage for an unrelated revision advancing between generation and save, including canonical reconciliation, bounded retry, and one persisted concept.
- Adds a two-client regression path that forces a genuine bank conflict, verifies the proposal remains intact, and verifies a deliberate retry uses the same identifier and creates no duplicate.
- Adds API coverage confirming AI proposal generation does not mutate the shared server revision or Top 3 concept state.
- Adds a homepage layout contract that prevents the Explore prompt from returning to absolute positioning outside the hero content flow.
- Confirms rendered subtitle-to-Explore separation with no horizontal overflow at 1440x900, 1024x768, 768x1024, 390x844, and 360x640 viewports, plus enlarged browser text.
- Adds deterministic calendar contracts for March, a non-March month, January, December, and both cross-year navigation directions.
- Adds static and loopback-only browser coverage for multiple collapsed show cards, independent expansion, edit-button access, and reload reset without persistence mutations.
- Extends the loopback-only Show Management journey to open scheduled and unscheduled full-show views, verify their slot-specific and no-slot metadata, preserve their respective URL routes, and prove the assignment map is unchanged.
- Adds a loopback-only browser journey that verifies the Schedule Board's December initialization, January navigation, return navigation, and reload reset while blocking unexpected external requests.
- Passes synchronized-entry-point, required-reference, Markdown-link, diff-whitespace, and stale-process-language checks for the Solo Development documentation alignment.
- Passes release-contract validation, parsing of every GitHub Actions workflow, and all 30 focused delivery, release, repository-security, and version tests for the process/workflow change.
- Passes pull-request validation with the complete Python 3.12 suite, fresh isolated PostgreSQL migrations, browser contract checks, production-image inspection, migration rollback/recovery, backup restore, and runtime recovery checks.
- Passes the isolated development deployment and authenticated environment smoke checks; public health reports `development`, version `0.0.7`, and the exact deployed branch commit.
- Adds repository-document checks that keep the AI entry points identical, require the canonical testing references, and reject broken local documentation links.
- Adds zero-retry Chromium coverage for the responsive public hero and the unauthenticated Show Management boundary while blocking unexpected external network requests.
- Establishes the overall Python coverage ratchet at the complete isolated-suite measurement of 63.06% and changed executable application lines at 90.0%; either floor may stay fixed or rise, but cannot be lowered merely to pass CI.
- Passes the complete isolated PostgreSQL suite with 326 tests, the measured coverage and changed-line gates, both Playwright journeys, static frontend contracts, migration validation, production-image inspection, rollback/recovery, backup restore, and runtime recovery.
- Confirms the cumulative branch deploys to isolated development and public health reports `development`, version `0.0.7`, and the exact deployed commit.
- Passes cumulative pull-request validation for universal scheduled and unscheduled full-show view mode with 326 isolated PostgreSQL tests, Alembic head `0009`, 63.06% overall coverage, four zero-retry Chromium journeys, production-image inspection, migration rollback/recovery, backup restore, and runtime recovery.
- Adds storage-contract coverage for canonical success notifications, failure rollback, conflict reload, subscription cleanup, and release-date persistence that does not mutate the visible cached slot before acknowledgement.
- Adds a loopback-only browser journey that saves a draft through the real control without reloading, rejects a conflicted optimistic draft, repaints a concurrent canonical draft, and verifies the same state after reload while blocking external requests.
- Adds an isolated database regression proving a scheduled idea content update preserves its assignment, slot dates, status, and reload result, plus a loopback-only browser journey covering the shared past, future, and unscheduled edit workflow through save, cancel, and reload.
- Pull-request validation run `32905693546` passes for universal show editing at commit `f07f23f517fa18f0e1e3c72759414b2975e1b5a6`: 327 isolated PostgreSQL tests, 63.06% overall coverage with changed application lines not applicable, six zero-retry Chromium journeys, Alembic head `0009`, production-image inspection, migration rollback/recovery, backup restore, and runtime recovery.
- The first #49 CI run exposed a repaint callback that re-entered slot initialization after a failed save and stacked error toasts. Reconciliation is now render-only, with a regression assertion forbidding `renderScheduleBoard()` from the storage notification callback; the original failure is retained as evidence.
- Pull-request validation run `31346017023` passes for the corrected #49 implementation commit `d0e1bb45175a06c3a95d2a7d92b4ebc3a06de73c`: 326 isolated PostgreSQL tests, 63.06% overall coverage, all five zero-retry Chromium journeys, Alembic head `0009`, production-image inspection, migration rollback/recovery, backup restore, and runtime recovery.
- Passes the focused frontend and Guest Bank contracts, JavaScript syntax, release/document validation, Python compilation, and the safe local suite with 209 tests passed and 117 database tests skipped because no explicit isolated `TEST_DATABASE_URL` is configured; CI provides the required isolated database, browser, and container layers.

## Deployment/Migrations

- No schema migration or data-contract migration is required; the existing Alembic head remains revision `0009`.
- The canonical repaint contract is frontend-only and requires no environment, secret, API, serializer, container, or workflow change.
- Universal show editing changes no schema, migration, API, serializer, persisted data contract, environment configuration, secret, container, or deployment workflow.
- Development and later approved environments must report version `0.0.7` and the exact deployed commit through health metadata.
- The production job now requires read-only Actions metadata access so it can prove the exact tagged commit completed the isolated test deployment before any production connection.
- Pull-request validation now installs a pinned browser runner and retains failure-only browser artifacts for seven days; no schema or runtime configuration changes are introduced.

## Rollback

- Redeploy the previously validated application commit for the browser changes; no database rollback is required. Correct a broken provenance query through the normal reviewed workflow path rather than bypassing the fail-closed production gate.

## Known Limitations

- A real concurrent Top 3 Bank change intentionally requires the user to review the refreshed canonical bank and retry the preserved proposal.
- Live AI generation remains dependent on a separately configured, environment-specific server-side provider credential; automated validation mocks the provider.
- On short viewports the hero grows with its content, so the Explore prompt may require scrolling rather than being forced over the subtitle.
- The Schedule Board does not persist a preferred viewing month; reopening or reloading intentionally returns to the current local month.
- Show-card expansion is intentionally session-only and resets to collapsed on reload.
- An unscheduled full-show view intentionally omits episode number, recording/release dates, and next-recording context until a schedule slot is assigned.
- Canonical repainting occurs after a page submits a mutation or reloads; this child does not add live cross-tab or server-push synchronization for changes made by another browser.
- Production promotion now depends on GitHub Actions run metadata being available; an API or permission failure blocks production before SSH.
- Automated browser coverage protects the public responsive hero, unauthenticated Show Management boundary, current-month Schedule Board behavior, collapsed cards and universal view access, canonical mutation reconciliation, and the shared past/future/unscheduled edit workflow; later UI children must extend the applicable parent journeys.
